1. test.html 파일 생성 후
git add .
git commit -m ""study: conflicts test1"
git push

2. test branch 만들기
git checkout -b test

3. master에서 파일 수정 후
git add .
git commit -m "study: conflicts test2"

4. test 브랜치로 이동
git checkout test

5. test 브랜치에서 파일 수정 후
git add .
git commit -m "study: conflicts test3"

6. merge 하기
git checkout main
git merge test

7. 충돌 해결
git commit -am "study: conflicks solve"
git push